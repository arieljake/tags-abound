
var connectMongo = require('connect-mongo');

module.exports.submissionDAO = require('./submissionDAO');
module.exports.submissionREST = require('./submissionREST');
module.exports.listDAO = require('./listDAO');
module.exports.listREST = require('./listREST');

module.exports.mapReduce = require('./mapReduce');
module.exports.utils = require('./utils');
module.exports.authRequired = require('./authRequired');

module.exports.settings = {
	getSessionSettings: function (express)
	{
		var mongoStore = connectMongo(express);

		return {
			secret: "listlyisfun",
			store: new mongoStore({
				db: "listly",
				collection: "v1_session",
				host: "127.0.0.1",
				port: 27017,
				auto_reconnect: true,
				clear_interval: 300,
				stringify: true
			})
		};
	}
};