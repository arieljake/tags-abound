


module.exports.createActivityForSubmission = function (submission, callback)
{
	var activity = createBaseActivity();
	activity.description = "New Submission: " + submission.title;

	callback(activity);
};


var createBaseActivity = function ()
{
	return {
		timestamp: (new Date()).toJSON()
	};
};